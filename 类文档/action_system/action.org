#+Title:Action

* 描述
Action即行动类，是负责执行行动的实现。Action类通常由ActionFactory创建，并带有玩家的id、level_handler等内容。

* 信号
** failed
当前行动执行失败时通知。

** finished
当前行动执行结束并执行成功时通知。

* 方法
** bool execute()
执行该行动，即运行该行动对应的Action Logic方法。
